# automatizacion

Single-file Python 3 script (`script.py`) — stdlib only, no dependencies.

## Run

```bash
python3 script.py
```

## Repo facts

- No package manager, no requirements, no venv.
- No test runner, linter, formatter, or type checker configured.
- Single commit on `main` pushed to `origin`.
